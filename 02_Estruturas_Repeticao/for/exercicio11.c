#include <stdio.h>

int main()
{
       int cont; 
    for (cont = 0; cont < 20; cont++){
        double valor;
        printf("Digite um valor com ponto flutuante: ");
        scanf ("%lf", &valor);
    }

    return 0;
}
